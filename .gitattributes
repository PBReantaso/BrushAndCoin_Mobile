mobile filter=lfs diff=lfs merge=lfs -text
path/to/your/large/file.ext filter=lfs diff=lfs merge=lfs -text

# Normalize text files and enforce LF in repo; allow CRLF on Windows working copies
* text=auto eol=lf

# Treat generated Android/Flutter build artifacts as binary (no EOL conversions)
mobile/build/** -text
mobile/android/**/build/** -text
mobile/.dart_tool/** -text

